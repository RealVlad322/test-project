взять порты из окружения и настройки ребита и монго
if (!shedules.length) {
        throw new Error('Empty data');
      } вынести на уровень обработки

bulkwrite

htpp-service 

сделать sync-link и вынести скорны в отдельный crone microservice

подумать над оптимизацией